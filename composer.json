{
  "name": "drupal/paragraphs_iframe_preview",
  "description": "Renders paragraph preview in closed mode using the frontend theme inside an iframe.",
  "type": "drupal-module",
  "license": "GPL-2.0-or-later",
  "require": {
    "drupal/core": "^8 || ^9 || ^10 || ^11",
    "drupal/paragraphs": "^1.0"
  }
}
